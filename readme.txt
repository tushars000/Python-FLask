Website using flask python framework

A simple Website which register users and allow them to login with the same credentials only if the credentials match with the data stored in database.
  
libraries used- 
1)Flask ;- framework
2)wtforms :- for validators in regsitration form
3)mysql :- to connect to my database 
3)passlib :- to encrypt/dycrypt the password in database
